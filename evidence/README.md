# Evidence provenance

`development/` contains actual Chromium executions of a **hand-authored development capability**. The automated handoff test uses a simulated operator. These files demonstrate browser behavior and testing, but do not satisfy the assignment's genuine LLM discovery requirement or prove a human manually completed takeover.

Genuine discovery and subsequent generated-artifact replay evidence remain pending. CLI runs are stored in `runs/` until selected and reviewed for the public submission.
